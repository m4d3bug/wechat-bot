module.exports = {
    // Special characters
    replaceRegex: /[ `~!@\$\^\&\*\(\)=\|{}':;',\[\]\.<>\/\?~！@￥……&\*（）——\|{}【】‘；：”“'。，、？]/g,

    // iPad token
    weChat: {
        token: 'token'
    },

    // Login status persistent
    botName: 'demo',
    // listen room
    listenRoomName: [
        'group name',
    ],
    // Allow list
    allowList: [],
    // Deny list
    denyList: [
        'fuck',
    ]
}

